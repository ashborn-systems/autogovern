# wordfreq

A small library for counting word frequencies in text. No agent, no LLM, no MCP — just string processing.

## Usage

```python
from wordfreq import count_words
count_words("the quick brown fox")
```
